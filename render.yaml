services:
  - type: web
    name: gz-mock-broker
    runtime: python
    rootDir: webapp
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
