{
  "mcpServers": {
    "mock-broker": {
      "command": "uv",
      "args": ["--directory", "mock-broker-mcp", "run", "server.py"],
      "env": {
        "BROKER_API_URL": "https://gz-mock-broker.onrender.com"
      }
    }
  }
}
